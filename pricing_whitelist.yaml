stores:
  byggmakker:           {domain: byggmakker.no}
  monter:               {domain: monter.no}
  maxbo:                {domain: maxbo.no}
  byggmax:              {domain: byggmax.no}
  obsbygg:              {domain: obsbygg.no}
  xlbygg:               {domain: xl-bygg.no}
  byggtorget:           {domain: byggtorget.no}
  elektroimportoren:    {domain: elektroimportoren.no}
  megaflis:             {domain: megaflis.no}
  jula:                 {domain: jula.no}
  biltema:              {domain: biltema.no}
  rightpricetiles:      {domain: rightpricetiles.no}
